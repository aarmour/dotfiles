[color]
  ui = auto
[push]
  default = current
[alias]
  br = branch
  ci = commit
  co = checkout
  lg = !tig
  pf = push --force-with-lease
  s = status
  pushf = push --force-with-lease
[core]
  excludesfile = ~/.gitignore
  ignorecase = false
[include]
  path = ~/.gitconfig.local
